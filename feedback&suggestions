Curl command examples, complete basic walkthrough (like opscenter APIs).  Include examples from cqlsh/astra console. Fully graphic walkthrough. Schema explanation? 

"Try it out" and "execute" button in Swagger might need tutorial/detailed doc

If create table with wrong data, how to replace with same name? Example, created using raw data, but need to use json. Do we drop? Just avoid this and change name?

Astra cqlsh console slow. Intermittently times out/resets.  Standalone cqlsh necessary

Keyspace == namespace(?)  DatabaseID == clusterID(?) Not clear in documentation, where do we find and map information to rest calls? Consistency

RequestUrl doesn't align with Stargate URL, different hosts etc:

curl -X POST "https://stargate-swagger-k47ipa5x5q-uc.a.run.app%7BdatabaseId%7D-%7Bregion%7D.apps.astra.datastax.com/api/rest/v1/auth" -H  "accept: application/json" -H  "X-Cassandra-Request-Id: d341f349-e5db-46d2-9c90-bb9ebaa6f0fc" -H  "Content-Type: application/json" -d "{\"username\":\"stargate\",\"password\":\"datastax\"}"

curl --location --request GET 'https://8c5b4d48-7834-4dd5-8d60-668bac7f4a65-us-east1.apps.astra.datastax.com/api/rest/v2/namespaces/dilbert/collections/tickets/52311' --header 'X-Cassandra-Token: 97766208-5d00-4989-b7e1-205ddf5c4f5a' --header 'Content-Type: application/json' | jq
